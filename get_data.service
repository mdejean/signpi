[Unit]
Description=Get data for sign
After=network.target
# not network actually being available, so this will ~always fail
Requires=sign_startup.service

[Service]
Type=oneshot
ExecStart=/usr/local/lib/signpi/get_data.py
WorkingDirectory=/usr/local/share/signpi/
